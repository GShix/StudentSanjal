# Application settings
APP_NAME=StudentSanjal
APP_ENV=local
APP_KEY=base64:3hapVI+340yv4B63c52MXCnXS2+MtVfnaxwpOGyu/rw=
APP_DEBUG=true
APP_TIMEZONE=UTC
APP_URL=http://127.0.0.1:8000
APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US
APP_MAINTENANCE_DRIVER=file

BCRYPT_ROUNDS=12

# Logging settings
LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

# Database settings (local database)
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=newproject
DB_USERNAME=root
DB_PASSWORD=

# Session settings
SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

# Cache settings
CACHE_STORE=database
CACHE_PREFIX=studentsanjal_cache

# Redis settings
REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

# Broadcasting & Pusher settings
BROADCAST_CONNECTION=pusher
BROADCAST_DRIVER=pusher
PUSHER_APP_ID=1883348
PUSHER_APP_KEY=1b75896478dd3b514d56
PUSHER_APP_SECRET=a5623d1550074ee084ad
PUSHER_APP_CLUSTER=ap2
PUSHER_HOST=api-${PUSHER_APP_CLUSTER}.pusher.com
PUSHER_PORT=443
PUSHER_SCHEME=https
PUSHER_APP_ENCRYPTED=true

# Mail settings (log mail for local)
MAIL_MAILER=log
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

# AWS settings (if needed)
AWS_DEFAULT_REGION=us-east-1
AWS_USE_PATH_STYLE_ENDPOINT=false

# Vite and Pusher settings
VITE_APP_NAME="${APP_NAME}"
VITE_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
VITE_PUSHER_HOST="${PUSHER_HOST}"
VITE_PUSHER_PORT="${PUSHER_PORT}"
VITE_PUSHER_SCHEME="${PUSHER_SCHEME}"
VITE_PUSHER_APP_CLUSTER="${PUSHER_APP_CLUSTER}"

# Google OAuth (local redirect URI)
GOOGLE_CLIENT_ID=862253961461-g2i7rccs1vmi8qaqql3rujbm5rnrqrss.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=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
GOOGLE_REDIRECT=http://127.0.0.1:8000/auth/google/callback
